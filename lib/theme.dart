// Base
const BASE_BORDER_RADIUS = 4.0;

// Body
const BODY_PADDING = 15.0;

// Bottom Sheet

// @todo bottom sheets have a small issue related to the border radius - all the clipped content is non-transparent, so
//       it looks kinda weird when the bottom radius is high enough
const BOTTOM_SHEET_BORDER_RADIUS = BASE_BORDER_RADIUS;

// Card
const CARD_BORDER_RADIUS = BASE_BORDER_RADIUS;

// Dialog
const DIALOG_BORDER_RADIUS = BASE_BORDER_RADIUS;
const DIALOG_PADDING = 24.0;

// TabView
const TAB_VIEW_PADDING = 6.0;
